footer.footer
  .container
    p.text-muted
      | Copyright goes here
      if !!footerLinks
        ul.footer-links
          for link in footerLinks
            li.text-center
              a(href=link.url, target="_blank") #{link.text}
